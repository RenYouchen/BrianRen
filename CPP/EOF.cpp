#include <bits/stdc++.h>
using namespace std;

int main() {
    int a;
    while (1) {
        cin >> a;
        if (a == EOF) {
            cout << "EOF" << '\n';
            break;
        } else {
            cout << a << '\n';
        }
    }
}
